/**
 * Runs once per test worker before any test file.
 * Intentionally empty for now — per-test isolation is handled by
 * `isolatedStorage` in vitest.config.ts, not by manual teardown here.
 */
export {}
